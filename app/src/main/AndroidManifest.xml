<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.fbartnitzek.tasteemall"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>
        <!--<meta-data-->
            <!--android:name="com.google.android.geo.API_KEY"-->
            <!--android:value="@string/GoogleMapsApiKey"/>-->

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <provider
            android:name=".data.DatabaseProvider"
            android:authorities="fbartnitzek.tasteemall"/>

        <!-- bugfix for editText hides nestedScrollView: -->
        <!-- android:windowSoftInputMode="adjustResize" -->
        <activity
            android:name=".AddProducerActivity"
            android:label="@string/title_add_producer_activity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize">
        </activity>
        <activity
            android:name=".AddDrinkActivity"
            android:label="@string/title_add_drink_activity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize">
        </activity>
        <activity
            android:name=".AddReviewActivity"
            android:label="@string/title_add_review_activity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize">
        </activity>
        <!--TODO: parent = back-button OR Home-Symbol...-->
        <activity
            android:name=".ShowProducerActivity"
            android:label="@string/title_activity_producer"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.fbartnitzek.tasteemall.MainActivity"/>
        </activity>
        <activity
            android:name=".ShowDrinkActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.fbartnitzek.tasteemall.MainActivity"/>
        </activity>
        <activity
            android:name=".ShowReviewActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.fbartnitzek.tasteemall.MainActivity"/>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_settings"
            android:parentActivityName=".MainActivity"
            android:theme="@style/SettingsTheme">
            <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.example.fbartnitzek.tasteemall.MainActivity"/>
        </activity>
    </application>

</manifest>